host_security_groups:
  - group: "k8s-worker{{ suffix }}"
    port: 30000
    port_end: 32767
    remote_ip_prefix: "0.0.0.0/0"
  - group: "k8s-worker{{ suffix }}"
    port: 10250
    #remote_group: "k8s-master{{ suffix }}" 
  - group: "k8s-worker{{ suffix }}"
    port: 10255
    #remote_group: "k8s-master{{ suffix }}" 

cloud_config:
  ssh_authorized_keys:
    - github:heytrav
    - github:Kodiack
    - github:catalystfd
  k3os:
    server_url: "https://202.49.242.163:6443"
    token: "{{ worker_token }}"

